version: '3'
services:
  mysql:
    image: mysql:latest
    environment:
      TZ: Asia/Shanghai
      MYSQL_ROOT_PASSWORD: 166232gsh
      MYSQL_DATABASE: blue
      MYSQL_USER: root
      MYSQL_PASSWORD: 166232gsh
    

  redis:
    image: redis:latest

  blue:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    container_name: blue
    depends_on:
      - mysql
      - redis
    
